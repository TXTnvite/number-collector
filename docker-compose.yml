# docker-compose.yml
version: '3.8'

services:
  react-app:
    build: .
    ports:
      - "80:80"
    depends_on:
      - nginx
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
